__pycache__/
*.pyc
*.pyo

# Raw memory-calibration observations (apple_silicon_nodes/memory_calibration.py).
# Promoted into the versioned apple_silicon_nodes/memory_evidence.json deliberately
# via scripts/promote_memory_evidence.py -- this staging file itself is per-machine
# noise, not meant to be reviewed/shared.
apple_silicon_nodes/.memory_evidence_staging.jsonl

# Local agent-tool artifacts (not part of the project)
.serena/
.claude/.headroom_wrap_marker.json
graphify-out/

# Local session notes/memory index -- not meant to be public
MEMORY.md
session-state.md
ROADMAP.md

# WIP nodes pending revisit, not registered -- kept locally, not published
disabled_nodes/

# Build artifact from local `uv`/pip builds
*.egg-info/

# speckit / spec-driven-development tooling -- per-machine, not part of ASDX
.claude/rules/
.claude/skills/speckit-*/
.specify/

# graphify / mempalace working data -- regenerated locally, not published
entities.json
mempalace.yaml
