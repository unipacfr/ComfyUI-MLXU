# Krea2 Normalized Attention Guidance (NAG) Implementation Plan

> **Status (2026-09-14): retired.** Fully implemented and wired into `ASDX_MLXSampler`
> (see git log `af0be8a`..`8890dc7` for the full build), then **removed** from the sampler
> in commit `3cbd51a` after a real smoke test showed it produced no targeting of the
> negative concept — the feature didn't work as intended, not an implementation bug caught
> in review. The code itself (`native/krea2/nag.py`, `predict_nag()`) is kept in the tree,
> unwired, in case a future attempt wants to start from it rather than from scratch. See
> memory `project-krea2-nag` for the short version. This plan is closed; no remaining tasks.

**Goal (historical):** Give Krea2 and Krea2 Identity Edit a working negative prompt in
ASDX via NAG (Normalized Attention Guidance), without a second sampler eps pass.

**Spec:** `docs/superpowers/specs/2026-09-10-krea2-nag-design.md` (commit `af0be8a`)
